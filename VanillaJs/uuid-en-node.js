


import { randomUUID } from 'crypto';

const generarId = () => {
    return randomUUID();
}

export default generarId;


//Este codigo genera en node y de forma nativa un identificador unico en uuid
//Para ejecutarla, debes llamar a la funcion pero sin los parentesis
